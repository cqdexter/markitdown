# 2026-04-20

## MarkItDown Visualizer - 设置面板重构

- 重写 `llm-ocr-settings.tsx`：大模型 API 设置改为折叠面板，默认 DeepSeek，支持多 API 保存/切换/删除
- 更新 `types/llm.ts` 和 `lib/llm-settings.ts`：支持多 LlmApiConfig 存储（v2 key）
- 恢复 PaddleOCR 设置区域（上次重写时意外删除），以独立折叠卡片形式呈现
- 更新大模型排版提示词：新增"去掉无用符号"和"去掉页码"两条规则
- 删除右上角 LLM OCR 复选框及相关逻辑（`useLlmOcr`、`llmConnectionStatus`、`checkLlmConnection`、自动切换 `/api/convert-ocr` 的逻辑），图片文件现统一走 `/api/convert` 或 PaddleOCR 路径
- 修复 Hydration 错误：`getDefaultDeepSeekConfig()` 改用固定 id `default_deepseek`（不用 Date.now()）；新增 `genConfigId()` 用于动态添加时生成唯一 id；`llm-ocr-settings.tsx` 和 `conversion-workspace.tsx` 的 localStorage 初始值改为 useEffect 挂载后同步，避免 SSR/客户端不一致
- 修复 `loadLlmOcrSettings is not defined` 报错：删除 LLM OCR 复选框时遗漏了 `handleLlmValidation` 中的引用，改为 `getActiveConfig()`（从 `lib/llm-settings.ts` 导入），大模型排版功能恢复正常

## 项目合并与部署准备

- 将 `markitdown-visualizer` 合并到 `markitdown` 仓库作为子目录
- 添加根目录 `vercel.json` 和 `requirements.txt` 用于 Vercel 部署
- 更新 `README.md` 添加部署说明章节
- Git 提交：b8df807 - feat: 添加 markitdown-visualizer Web 可视化界面
